import 'package:flutter/material.dart';

class AppColors {

  static const Color whiteColor = Colors.white;
  static const Color scaffoldDark = Color(0xFF0A0E17);
  static const Color cardDark = Color(0xFF141927);
  static const Color surfaceDark = Color(0xFF1A1F33);
  static const Color elevatedDark = Color(0xFF1E2540);

  // ── Primary ──
  static const Color primary = Color(0xFF00C853);
  static const Color primaryLight = Color(0xFF69F0AE);

  // ── Charging State Colors ──
  static const Color chargingRed = Color(0xFFFF073A);
  static const Color chargingRedGlow = Color(0xFFFF1844); // Neon electric hot red-pink (Image 1)
  static const Color chargingRedLight = Color(0xFFFF7694);
  static const Color chargingRedDark = Color(0xFF9E0022);

  static const Color chargingOrange = Color(0xFFFF7F00);
  static const Color chargingOrangeGlow = Color(0xFFFF7A00); // Blazing neon electric orange (Image 3)
  static const Color chargingOrangeLight = Color(0xFFFFD180);
  static const Color chargingOrangeDark = Color(0xFFB34A00);

  static const Color chargingGreen = Color(0xFF00FF66);
  static const Color chargingGreenGlow = Color(0xFF2EFE58); // Hyper neon electric green (Image 2)
  static const Color chargingGreenLight = Color(0xFFB9F6CA);
  static const Color chargingGreenDark = Color(0xFF007A33);

  // ── Text ──
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E93A7);
  static const Color textTertiary = Color(0xFF5A5F72);
  static const Color textHint = Color(0xFF4A4F62);

  // ── Status ──
  static const Color success = Color(0xFF00FF66);
  static const Color warning = Color(0xFFFF7F00);
  static const Color error = Color(0xFFFF073A);
  static const Color info = Color(0xFF5AC8FA);

  // ── Bottom Nav ──
  static const Color navActive = Color(0xFF00C853);
  static const Color navInactive = Color(0xFF5A5F72);
  static const Color navBackground = Color(0xFF0D1120);

  // ── Borders & Dividers ──
  static const Color border = Color(0xFF2A2F42);
  static const Color divider = Color(0xFF1E2333);

  // ── Slide Button ──
  static const Color slideTrack = Color(0xFF1A1F33);
  static const Color slideThumb = Color(0xFFFF073A);

  // ── Bolt / Lightning ──
  static const Color bolt = Color(0xFFFFD600);
}
