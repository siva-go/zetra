// Basic test placeholder for the ZETRA app.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts', (WidgetTester tester) async {
    // Placeholder — real widget tests to be added later.
    expect(1, equals(1));
  });
}
